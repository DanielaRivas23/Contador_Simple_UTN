import { extendTheme } from '@chakra-ui/react';

const theme = extendTheme({
  colors: {
    brand: { 
      primary: '#47634d', 
      secondary: '#627967',
      accent: '#dce1dd',
      hover: '#efd528',
    },
  },
});

export default theme;
